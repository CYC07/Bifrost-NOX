How to use on Windows:
  1. Copy the entire ai_firewall folder to your Windows machine.
  2. Double-click `install_windows.bat` (wait for it to finish).
  3. Right-click `start_windows.bat` and choose Run as Administrator.
  4. Set your Windows Proxy settings to 127.0.0.1:8080 to route web traffic through it.

