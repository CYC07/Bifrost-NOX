# Regenerable — rebuild with `python -m ml.waf.build_dataset` / `train`
artifacts/
data/build/
# Your ingested data (downloads / VulnHub / writeups) — add deliberately
data/external/*
!data/external/.gitkeep
# Captured live traffic — contains real request URLs/bodies, never commit
shadow_verdicts.jsonl
shadow_clean.jsonl
